import {StyleSheet} from 'react-native';
import { FONTS } from '../../Assests/Themes';

const styles = StyleSheet.create({
 input: {
    //flex: 1,
    fontSize: 15,
    color: 'black',
    borderWidth: 1,
    backgroundColor: 'white',
    borderRadius: 10,
  },
});
export default styles;
